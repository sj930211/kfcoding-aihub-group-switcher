# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 已登录 KFCoding、AIHub、ooioo 或 FluxionAI，并使用一个或多个 API 密钥的个人用户。
- 用户需要在供应商页面内同时了解当前渠道健康、路由成本、账户用量和自动切换状态。

## Product Purpose

油猴脚本在当前供应商页面中提供一个紧凑的运维控制台，读取分组倍率、性能监测、账号可选范围和用量数据，并在用户授权的策略与保护规则内管理一个或多个 API 密钥的分组。

统计能力用于回答两个独立问题：账户每天消费如何变化，以及各密钥在真实供应商用量数据中的使用占比。成功标准是口径明确、数据可追溯，不用估算值冒充供应商账单。

## Positioning

插件把供应商公开监测、当前账号可路由倍率、密钥分组状态、自动切换保护和真实用量放在同一个页面内，并对数据来源、回退和缺失状态做明确标记。

## Operating Context

- 插件只在相应供应商页面打开时运行。
- 面板当前使用 `监控 / 统计 / 诊断 / 设置` 四个工作区。
- 每个站点的配置、日志、位置、保持记录和统计数据相互隔离。
- 日期以浏览器本地时区的自然日划分。

## Capabilities and Constraints

- 统计页默认显示当前站点，不合并四个站点的金额。
- 支持消费金额、请求数和 Token 三种指标，默认使用实际消费金额。
- 每日趋势默认显示今天，并允许查看昨天及最近 7、14 和 30 天；今天只展示已经完成的本地小时区间并每 5 分钟刷新一次，昨天固定展示前一自然日完整 24 小时且不自动刷新。
- 今天的小时趋势与密钥日累计不直接对账；覆盖率、未分配量和异常判断使用同一天的账户日汇总，日汇总不可用时明确显示待对账。
- 每个密钥使用度使用横向柱状图，按当前指标降序排列。
- 只有供应商提供真实按密钥数据时才展示按密钥消费；如果只提供账户总消费，则明确显示暂无按密钥数据，不平均分摊或自行估算。
- 供应商没有历史接口时，可从插件成功读取用量之日起保存每日聚合快照；本地默认保留 90 天。
- 本地统计只保存站点、日期、密钥内部 ID、聚合用量、来源和采集时间，不保存或显示完整 API Key、access token、refresh token 或 Authorization。
- AIHub 已确认存在账户日趋势与单密钥 1–90 天真实日明细，覆盖实际消费、请求数和 Token；金额以美元展示，日期按显式浏览器时区的自然日计算。
- AIHub 的批量密钥用量接口只提供今日与默认最近 30 天的实际消费，不作为 7/14/30 天统一筛选或请求数/Token 图表的数据源。
- KFCoding、ooioo 和 FluxionAI 的历史日用量、按密钥明细、账单对账、货币和时区接口能力仍待分别核实。

## Evidence on Hand

- 现有用户脚本与说明：`kfcoding-group-switcher.user.js`、`README.md`。
- 当前实现已提供账户余额、今日消费、今日请求和今日 Token，但尚未提供历史趋势或按密钥用量图表。
- 接口能力必须通过当前供应商页面的只读请求确认，不能从单一站点外推。

## Product Principles

- 真实数据优先：供应商未提供的数据保持不可用，不制造精确幻觉。
- 当前任务优先：监控负责即时决策，统计负责历史理解，两者信息架构分离。
- 数据来源透明：实际账单、供应商统计、本地快照和插件观测必须清楚区分。
- 密钥安全：只使用内部 ID 和名称，不持久化或展示完整凭证。
- 站点隔离：不同供应商的货币、时区和用量口径分别呈现。

## Accessibility & Inclusion

- 图表必须提供文本摘要、键盘聚焦和可访问标签，不能只依赖颜色表达数值或数据状态。
- `0`、未知值和接口不可用必须使用不同文案与视觉状态。
