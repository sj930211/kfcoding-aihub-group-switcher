{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs build",
    "test": "npm run build && node --check kfcoding-group-switcher.user.js && node test-selection.mjs"
  }
}
